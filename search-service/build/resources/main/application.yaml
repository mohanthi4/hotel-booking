spring:
  application:
    name: hotel

  mongodb:
    uri: { $SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/hotelsDB }

  data:
    redis:
      host: localhost
      port: 6379

  cache:
    type: redis
